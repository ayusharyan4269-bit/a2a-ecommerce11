node_modules/
.next/

*.log
dist/
.DS_Store
Thumbs.db
tsconfig.tsbuildinfo

.vercel

/src/generated/prisma

# ── Environment & Secrets (NEVER commit these) ──────────────────────────────
.env
.env.local
.env.*.local
.vault-key
.vault-state.json

# ── Hardhat build artifacts (generated, not needed in git) ─────────────────
artifacts/
cache/
typechain-types/
